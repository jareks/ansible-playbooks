---
# vars file for jareks.devstrap.common
dev_user_id: 1000
# default values will work fine in local mode, but in ssh mode, you need to override those
homedir: "{{ lookup('env','HOME') }}"
dotfiles_dir: "{{ homedir }}/dotfiles"
